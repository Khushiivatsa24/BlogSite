flask db migrate- to make changes
flask db upgrade apply the changes
jinja 
rendering
sqlalchemy 
created a database, a form, did routing 
your brain tends to remember the patterns that's what i have realized i might think idk much which i dont but still i can connect the dots
esp 32 temperature project dashboard 

jinja2- templating engine for python
{%conditional statements %} why do we need it because without it html is completly static

pagination- means you limited number of stuff then next page. 
jwt0 json web token (verified on its own)
flask-moment (used for js)
ajax asynchronous javascript and xml (usually json)
*args - collects extra positional arguments as a tuple
**kwargs - extra keyword arguments as a dictionary